# Separate from tests/: that suite stubs Home Assistant in its conftest.py,
# this one needs the real thing (pytest-homeassistant-custom-component).
# Run from this directory:  cd tests_ha && pytest
[pytest]
pythonpath = ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
